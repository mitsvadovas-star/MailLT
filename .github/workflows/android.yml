name: Build MailLT APK

on:
  workflow_dispatch:
  push:
    branches: [ "main", "build/**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract MailLT source
        run: unzip -q MailLT-build-src.zip

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 35 SDK
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build debug APK
        working-directory: MailLT
        run: gradle --no-daemon assembleDebug

      - name: Prepare APK name
        run: cp MailLT/app/build/outputs/apk/debug/app-debug.apk MailLT-1.1-debug.apk

      - name: Upload Actions artifact
        uses: actions/upload-artifact@v4
        with:
          name: MailLT-debug-apk
          path: MailLT-1.1-debug.apk
          if-no-files-found: error

      - name: Publish GitHub release
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete v1.1 --yes --cleanup-tag || true
          gh release create v1.1 MailLT-1.1-debug.apk --title "MailLT 1.1" --notes "Android testavimo APK. IMAP/SMTP, kelios paskyros, bendras Inbox, paieška, Reply, Forward, priedų siuntimas ir saugus paskyros slaptažodžio saugojimas Android Keystore." --target "$GITHUB_SHA"
